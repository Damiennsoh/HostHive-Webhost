/**
 * Mock Authentication Configuration
 * Set MOCK_AUTH_ENABLED to false for production.
 */
export const MOCK_AUTH_ENABLED = false;

// Mock-related functions are deprecated and have been removed to ensure
// that only real Supabase authentication is used in the production environment.
